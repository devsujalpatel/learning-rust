fn main() {

    let x:i32 = -5; // number that can be both +ve and -ve
    let y:u32 = 1000; // number that can only be +ve
    let z:f32 = 33.001;  // number that can be both +ve and -ve but has decimal places

    print!("x: {}, y: {}, z: {}", x, y, z)

}
